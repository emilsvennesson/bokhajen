import { NavLink } from "react-router-dom";

const NavigationBar = () => {

    return(
        <nav className="nav">
            <p>BokTrading</p>
        </nav>
    );
}
export default NavigationBar;
